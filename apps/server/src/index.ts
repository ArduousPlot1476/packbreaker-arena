// @packbreaker/server — Fastify server.
//
// M1.1 stub. Populated in M1.5 with two endpoints per tech-architecture.md § 6.1:
//   GET  /v1/contract/daily       → DailyContractResponse
//   POST /v1/telemetry/batch      → 204
//
// No persistence, no auth, no DB in M1. Postgres + Redis arrive in M2.
export {};
