// @packbreaker/ui-kit — React component primitives (Button, Card, RarityFrame, BagCell, …).
//
// M1.1 stub. Populated lazily as components are extracted from apps/client during M1.3
// when App.tsx is split. Tailwind-based, depends on the locked palette tokens in
// apps/client/src/index.css (eventually promoted to a shared CSS layer).
export {};
