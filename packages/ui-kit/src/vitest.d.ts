// Loads @testing-library/jest-dom matcher type augmentation so
// .toBeInTheDocument(), .toHaveTextContent(), etc. typecheck inside
// the colocated *.test.tsx files. Runtime registration is in test/setup.ts.

/// <reference types="@testing-library/jest-dom" />
