{
  "name": "@packbreaker/shared",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "lint": "eslint src --max-warnings 0",
    "typecheck": "tsc --noEmit",
    "build": "tsc",
    "test": "echo \"no tests in M1.1 — shared is types-only stub\" && exit 0"
  },
  "dependencies": {
    "@packbreaker/content": "workspace:*"
  },
  "devDependencies": {
    "@packbreaker/eslint-config": "workspace:*",
    "@packbreaker/tsconfig": "workspace:*",
    "typescript": "^5.6.3"
  }
}
